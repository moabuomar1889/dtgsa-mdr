import { FoundationStatus } from "@dtg/ui"

export default function ApprovalFoundationPage() {
  return (
    <FoundationStatus
      title="Approval foundation"
      message="Approval application foundation — workflow implementation pending."
    />
  )
}
