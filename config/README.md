# Configuration

This directory is reserved for reviewed, non-secret, cross-deployment
configuration artifacts. Phase 2 keeps runtime environment ownership in each
application and does not add production configuration.
