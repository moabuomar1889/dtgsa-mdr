# Graphify Phase 12

Date: 2026-07-29

Phase 12 connects `@dtg/verification-domain`, `VerificationCode`,
`VerificationAttempt`, `VerificationRecord`, `PublicVerificationPolicy`,
`PackageManifest`, `PlatformSeal`, `SigningKeyRegistry`, controlled files,
client responses, generated artifacts, and the public/internal verification
applications.

```text
graphify query "How does a public verification code become a privacy-filtered result?"
```
