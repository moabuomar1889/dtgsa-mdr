# Timestamp Provider Interface

The initial provider records authenticated platform UTC time and supports
persisting database time. Its status is recorded, not trusted. The interface
has an RFC 3161 provider type for future activation, but no third-party trusted
timestamp is claimed or configured.
